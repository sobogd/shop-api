/**
 * Загружаем изображения, если есть, и добавляем остальные данные в БД.
 * @param body Тело объекта, который нужно записать.
 */
export const categoryEdit = async (body) => {
  
  return updateResult;
};
